upstream upstream_{{slug}} {
  server {{redirectIP}}:{{redirectPort}};
}

server {
  listen {{listenPort}};
  server_name {{serverName}};

  location / {
    proxy_pass $scheme://upstream_{{slug}};
    proxy_set_header Host $host;
  }
}
