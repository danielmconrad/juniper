module.exports =
  createOne: (config, slug) ->
  createAll: (config) ->
  removeOne: (config, slug) ->
  removeAll: (config) ->
  pullOne: (config, slug) ->
  pullAll: (config) ->
  sync: (config) ->
